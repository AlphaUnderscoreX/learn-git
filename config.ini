branch = 'first'
behavior = 'Python ini files behave same as Java properties file'

push_when_remote_already_contains_branch = 'test'
push_change = 'Simulating code advancement in remote'

blah = 'Some blah in config too'
